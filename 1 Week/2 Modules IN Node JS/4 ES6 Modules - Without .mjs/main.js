import { subtract, sum, divide, multiply } from "./arithmetic.js";
console.log(subtract(2, 3));
console.log(sum(2, 3));
console.log(divide(2, 3));
console.log(multiply(2, 3));


// We need to rename the file to use modules as .mjs instead of .js
